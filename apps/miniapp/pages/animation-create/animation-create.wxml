<view class="page"><view class="hero hero-soft"><text class="eyebrow">NEW ANIMATION</text><text class="hero-title">从故事创建动画</text></view><view class="card"><text class="card-title">动画标题</text><input value="{{title}}" maxlength="120" bindinput="inputTitle"/><button class="primary-btn" loading="{{submitting}}" disabled="{{submitting || !workId || !versionId}}" bindtap="submit">创建动画</button><text wx:if="{{error}}" class="state-copy">{{error}}</text></view></view>
