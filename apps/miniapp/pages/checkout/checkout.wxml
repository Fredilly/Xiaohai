<view class="page"><view wx:if="{{loading}}" class="state-card">结算加载中…</view><view wx:elif="{{error}}" class="state-card">无法结算，请检查登录、地址和商品状态</view><view wx:elif="{{!addressId}}" class="state-card">请先在“我的 → 地址”添加地址</view><view wx:else class="section"><text>服务端重新计算金额：¥{{preview.totalMinor/100}}</text><text>{{preview.paymentMessage}}</text><button bindtap="create">创建未支付订单</button><text>本页面不会调用微信支付，也不会模拟支付成功。</text></view></view>
